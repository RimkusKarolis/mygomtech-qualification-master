export enum Routes {
  Login = '/login',
  Users = '/items',
  Weak = '/items/weak',
  Reused = '/items/reused',
  Old = '/items/olg',
  Root = '/',
}

export enum API {
  Login = 'api/login',
  Items = 'api/items',
  User = 'api/user',
}
